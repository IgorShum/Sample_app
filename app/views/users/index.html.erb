<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |u| %>
    <li>
  <%= gravatar_for_user u, size:50 %>
      <%= link_to u.name, u %>
    </li>
  <% end %>
</ul>